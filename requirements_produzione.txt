# Requirements per sistema chiamate con XTTS v2

# Core TTS
TTS>=0.22.0
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# Audio processing
soundfile>=0.12.1
scipy>=1.10.0
librosa>=0.10.0
pydub>=0.25.1

# Web frameworks (per integrazioni telefoniche)
flask>=2.3.0
twilio>=8.0.0

# Utilities
numpy>=1.24.0
requests>=2.31.0

# Monitoring e logging
prometheus-client>=0.17.0

# Security
cryptography>=41.0.0

# Optional ma consigliati
# ffmpeg-python>=0.2.0  # Per conversioni audio avanzate
# uvloop>=0.17.0  # Per performance asyncio
# gunicorn>=21.0.0  # Per deployment produzione
